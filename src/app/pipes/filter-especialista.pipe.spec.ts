import { FilterEspecialistaPipe } from './filter-especialista.pipe';

describe('FilterEspecialistaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEspecialistaPipe();
    expect(pipe).toBeTruthy();
  });
});
