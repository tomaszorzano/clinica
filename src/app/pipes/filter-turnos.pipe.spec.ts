import { FilterTurnosPipe } from './filter-turnos.pipe';

describe('FilterTurnosPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTurnosPipe();
    expect(pipe).toBeTruthy();
  });
});
